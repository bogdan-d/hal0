# TTS slot — kokoro-onnx in a podman container (hal0-slot@tts), CPU-only.
name = "tts"
type = "tts"
device = "cpu"
runtime = "container"
profile = "tts"
enabled = true
port = 8084

[model]
default = "kokoro-v1"
