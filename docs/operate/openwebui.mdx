---
title: OpenWebUI
description: The bundled chat UI on :3001, prewired to the local hal0 API.
sidebar:
  order: 4
---

import { Aside } from '@astrojs/starlight/components';

hal0 ships **OpenWebUI** as the chat surface. The installer wires it
to the local hal0 API automatically — zero config, just open
`http://localhost:3001` after install.

## Why bundled

The dashboard is for operating the box (slots, models, hardware,
logs). It isn't a chat UI. OpenWebUI is a full chat client with
conversation history, model switching, RAG, prompt presets, and
voice mode, all under a permissive license.

Bundling means:

- **One install command, two UIs ready.** No "now go install
  OpenWebUI separately and figure out the API URL."
- **Same lifecycle.** OpenWebUI is a systemd unit, updates with
  `hal0 update`, logs into journald.
- **The env is right by default.** `OPENAI_API_BASE_URLS` points at
  `http://127.0.0.1:8080/v1`; `OPENAI_API_KEYS` is set to a local
  token.

## Where it lives

| Component                | Location                                       |
|--------------------------|------------------------------------------------|
| Service unit             | `hal0-openwebui.service`                       |
| Listen port              | `:3001` (binds public interfaces)              |
| State directory          | `/var/lib/hal0/openwebui/`                     |
| Env file                 | `/etc/hal0/openwebui.env`                      |

State (chats, prompts, RAG documents) lives under `/var/lib/hal0/`,
so `hal0 update` preserves your conversation history.

## Open it

```sh
xdg-open http://localhost:3001
```

On first launch OpenWebUI walks through its own account-creation
flow. After that, models from `/v1/models` populate the model
dropdown — both [slot aliases](/docs/api/slot-as-model/) and
registry refs.

## Customising

Standard OpenWebUI environment variables work; set them in
`/etc/hal0/openwebui.env` and restart the service:

```sh
sudo systemctl restart hal0-openwebui.service
```

The installer writes this file once at install time; it's not
overwritten on `hal0 update`.

<Aside type="note">
  `:3001` binds public interfaces by default with no auth, which is
  fine on a trusted LAN. For remote access, run
  `install.sh --auth=basic` to bring up the bundled Caddy reverse
  proxy. OpenWebUI gets single-sign-on through Caddy's basic_auth
  identity (no second login), and the bundled bearer-token auth gates
  the OpenAI API surface. See
  [Authentication & HTTPS](/docs/operate/auth/) for the full flow.
</Aside>

## Behind your existing reverse proxy

If your homelab already runs Traefik or Caddy in front of `*.lan` or
`*.thinmint.dev` services, point it at OpenWebUI directly instead of
using hal0's bundled Caddy. OpenWebUI emits root-absolute asset URLs
and has no base-path config, so give it its own hostname (or port).
A `PathPrefix` under your dashboard won't work; the assets will 404.

A typical Traefik dynamic config snippet:

```yaml
http:
  routers:
    hal0-chat:
      rule: "Host(`hal0-chat.lan`)"
      service: hal0-chat
      entryPoints: [websecure]
      tls: {}
  services:
    hal0-chat:
      loadBalancer:
        servers:
          - url: "http://<hal0-host>:3001"
```

Do auth at the proxy (forward-auth, OIDC middleware, basic_auth,
whatever you already trust). OpenWebUI's `WEBUI_AUTH_TRUSTED_EMAIL_HEADER`
will pick up the forwarded identity exactly like it does with hal0's
bundled Caddy. If you want OpenWebUI to be reachable only through the
proxy and not directly on `:3001`, firewall the port at the host or
front it with the proxy on a different interface.

## Turning it off

If you don't want OpenWebUI:

```sh
sudo systemctl disable --now hal0-openwebui.service
```

The hal0 API keeps running. The dashboard at `:8080` keeps running.
You can reach the `/v1/*` endpoints from any other client.

## Coming soon — outline

- Single sign-on between dashboard and OpenWebUI.
- Shared theme tokens so OpenWebUI inherits the hal0 amber.
- One-click voice-mode wiring (OpenWebUI → `stt` slot → `primary`
  slot → `tts` slot).
