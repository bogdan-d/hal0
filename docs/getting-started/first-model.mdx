---
title: Load your first model
description: Pull a model from Hugging Face into hal0's registry and assign it to a slot — through the first-run picker or with the hal0 model CLI.
sidebar:
  order: 30
---

import { Steps, Aside, Tabs, TabItem } from '@astrojs/starlight/components';

Models in hal0 live in a local registry, the single source of truth for
Hugging Face coordinates, SHA-256 digests, and curated filenames. You can
populate it through the first-run picker or directly from the CLI.

## Via the bundle picker

If you ran [first-run setup](/docs/getting-started/setup/) and chose a
tier, hal0 already seeded the registry and kicked off background pulls for
each model in the bundle. The install-progress pane reattaches to those
download streams and shows per-model progress. Nothing more is required —
when a pull completes, its slot is ready to start.

## Via the CLI

The `hal0 model` subcommand is a thin HTTP client to the API. The common
flow is **list → pull → assign**.

<Steps>

1. **List what's available** — registry rows plus models advertised by
   configured upstreams:

   ```sh
   hal0 model list
   ```

   Add `--json` to emit the raw `/api/models` payload for scripting.

2. **Pull a model** from Hugging Face. The argument is either a curated
   alias (e.g. `qwen3-4b`) or a registered model id:

   ```sh
   hal0 model pull qwen3-4b
   ```

   The pull runs as a background job on the daemon. The CLI polls
   `/api/models/<id>/pull/status` every 500 ms and prints a progress bar
   until the job reaches a terminal state, then reports the final path
   and SHA-256.

3. **Assign the model to a slot's default** (this does not start the
   slot):

   ```sh
   hal0 model assign qwen3-4b --slot chat
   ```

   This writes `model.default` into the slot's configuration. Start the
   slot afterward (see [Manage slots](/docs/guides/manage-slots/)).

</Steps>

Other `hal0 model` commands: `register` (record an on-disk file in the
registry), `show` (inspect metadata), and `rm` (remove a registry row).

<Aside type="tip" title="Gated or large models">
Pulls that need a Hugging Face token read `HF_TOKEN` from the API
environment. The easiest path is the dashboard: **Settings → Secrets →
HuggingFace token** applies live without a restart.
</Aside>

## When a pull fails

A pull job ends in one of these states: `queued`, `running`, `completed`,
`failed`, or `cancelled`. On `failed`, the CLI exits non-zero and prints
the error message returned by the daemon; the registry row stays in place
so you can retry.

```sh
# retry — an already queued/running pull for the same id is reused,
# not duplicated
hal0 model pull qwen3-4b
```

The FirstRun progress pane surfaces the same failure inline with a
**Retry** button, and you can always pull later from the dashboard's
**Models** view. Re-issuing a pull for a `completed`/`failed`/`cancelled`
job replaces it; a pull already `queued` or `running` returns the
existing job handle rather than starting a second download.

## Next

Once a model is pulled and assigned, you're ready to
[send your first chat](/docs/getting-started/first-chat/).
